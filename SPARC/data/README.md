# Public data directory

This directory contains unmodified public SPARC records. Use `../download_data.py` from the repository root to download and verify them against `../data_sources.json`.

The rotation-curve ZIP is checksum-verified before extraction, and the extractor requires exactly 175 `_rotmod.dat` members. Never alter the scientific files or silently substitute another release.
