# Public data directory

This directory contains unmodified public DESI catalogue files required by the audit. Files are large and may be omitted from source-control archives.

Use `../download_data.py` from the repository root to download and verify them against `../data_sources.json`. Never edit catalogue contents, silently replace releases, or bypass checksum verification.
