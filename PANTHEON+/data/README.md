# Public data directory

This directory contains the unmodified public Pantheon+SH0ES catalogue used by the audit. Use `../download_data.py` from the repository root to download and verify it against `../data_sources.json`.

Never edit the catalogue, replace it silently, or bypass checksum verification.
